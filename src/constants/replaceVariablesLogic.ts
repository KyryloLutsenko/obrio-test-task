export const REGEXP_PATTERNS = {
  PARENT: /{(parent|single-parent)}/g,
  VARIABLES: /{(\w+)}/g,
};

export const REPLACE_LOGIC_STRINGS = {
  HAS_CHILDREN: 'who have children',
  EMPTY: '',
  GENDER: 'gender',
  YES: 'Yes',
};
